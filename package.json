{
  "name": "movies-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot for search movies in my own movies database",
  "scripts": {
    "start": "node lib/main.js",
    "test": "jasmine JASMINE_CONFIG_PATH=jasmine.json",
    "build-with-watch": "babel --watch --presets es2015 -d lib/ src",
    "build": "babel --presets es2015 -d lib/ src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "node-telegram-bot-api": "^0.30.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "jasmine": "^3.4.0"
  }
}
